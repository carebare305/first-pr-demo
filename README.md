# first-pr-demo

A tiny scratch repo for practicing the GitHub pull request workflow.

## What this is

This repo exists so you can experience the full loop once: make a change,
open a pull request, and merge it — without touching real project code.

## Usage

Clone it, poke at `greet.py`, and open a PR with your change.
